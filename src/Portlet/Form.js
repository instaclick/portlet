define(
    [
        'jquery'
    ],
    function ($) {
        'use strict';

        var PortletForm = function () {};

        $.extend(PortletForm.prototype, {});

        return PortletForm;
    }
);
